on:
  push:
    tags:
      - "v*.*.*"

jobs:
  build_image:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - run: git tag --points-at HEAD
      - name: build image
        run: docker build -t test-git .
